const links = ["About Us", "Apply", "Sponsors"]
export default function Header () {
    return (
        <nav className= "bg-gray-800 flex justify-between items-center h-20 p-4">
            {/* Logo */}

    </nav>
    );
}
